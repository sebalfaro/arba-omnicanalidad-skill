interface:
  display_name: "Arba Omni"
  short_description: "Plan and drive feature work through delivery gates"
  default_prompt: "Use $arba-omni to plan and drive this feature from architecture through final code review."
policy:
  allow_implicit_invocation: true
